/// Asset path constants for the Alif mobile app.
///
/// Add image and icon paths here so they can be referenced type-safely
/// throughout the app instead of using raw string literals.
class AppImages {
  AppImages._();

  static const String _base = 'assets/images';

  static const String logo = '$_base/logo.png';
}
